重新考虑功能，拆分成原子功能：

1. 系统启动时 docker compose 恢复，目前看来意义不大？近期 fnos 版本似乎已经正常了，此功能需求有待进一步确认。
2. docker 容器重启，目前看来仅在旁路由宕机时需要。
3. docker 容器状态检测。配合功能 2。
4. 物理网卡状态监听。
5. macvlan-proxy 状态检测。
6. macvlan-proxy 到 docker 容器的可达性检测。
7. macvlan-proxy 状态重置。
8. （计划新增，实现方式待定）运行于主路由的应用：
    1. 旁路由网络可达性检测
    2. dhcp 旁路由 option 配置功能调整

目前设计中，1 跟其他功能无关。

2-7 耦合太紧密，而且依赖于不稳定的物理网卡状态监听（现实情况下的客观复杂度，非程序实现问题，）。计划调整一下。
调整思路大概是：收到通知，会延迟启动一个检测进程，并防止并发重入。检测发现网络不通后依次处理
1. 处理 macvlan-proxy 可用性
2. 处理 docker 容器

8 属于主路由应用，应该是独立做一个比较好。
